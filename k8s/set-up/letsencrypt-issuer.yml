apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt
spec:
  acme:
    # Staging server for testing; switch to production once verified
    server: https://acme-staging-v02.api.letsencrypt.org/directory
    email: ${ISSUER_NOTIFICATION_EMAIL}
    privateKeySecretRef:
      name: letsencrypt
    solvers:
      - dns01:
          cloudflare:
            email: iradukundaleekevin@gmail.com
            apiTokenSecretRef:
              name: cloudflare-api-token
              key: api-token
